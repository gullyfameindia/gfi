import React from 'react';
import CameraModule from '../camera-module';

/**
 * Root screen for the app.
 * Renders the self-contained camera module:
 * Home → Camera → Preview.
 */
export default function AppRootScreen() {
  return <CameraModule />;
}


